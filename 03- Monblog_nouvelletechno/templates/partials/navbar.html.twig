<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('accueil')}}">Accueil</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{ path('articles')}}">{% trans %}Articles{% endtrans %}</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('mentions')}}">{% trans %}Legale Notice{% endtrans %}</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('sitemap')}}">Sitemap</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('api_liste')}}">API</a>
        </li>
        {% if is_granted('ROLE_ADMIN') %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('admin_index')}}">Admin</a>
          </li>
        {% endif %}
        {% if is_granted('ROLE_USER') %}
        <li class="nav-item">
          <a class="nav-link" href="#">{% trans %}Profile{% endtrans %}</a>
        </li>
      {% endif %}
      </ul>
      <ul class="navbar-nav ml-auto mb-2 mb-lg-0">
       {% for locale in locales %}
        {% if locale != app.request.locale %}
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('change_locale', {'locale': locale}) }}"><img width="40px" height="auto" src="{{ asset('img/'~locale~'.png') }}" alt="{{locale}}"></a>
        </li>
        {% endif %}
       {% endfor %}
      </ul>
    </div>
  </div>
</nav>